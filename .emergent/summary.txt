<analysis>
The previous AI engineer successfully transformed a basic task manager into a multi-tenant Lumberyard House Building Tracker. Initial development established core CRUD, RBAC, and multi-tenancy via . Subsequent work focused on robust feature expansion. Key implementations include a comprehensive budget system for projects and rooms (with detailed rollup calculations), a multi-faceted user management system allowing viewing/reactivation of deactivated users and super admin password resets, and auto-population of subtasks based on room labels. Architectural decisions involved strict use of environment variables for URLs, FastAPI for the backend, React for the frontend, and MongoDB as the database. The engineer adeptly handled technical challenges like frontend-backend API prefix mismatches and refined budget logic to align with allocation-based accounting. The application now supports complex permissions, detailed financial tracking, and improved user experience.
</analysis>

<product_requirements>
The Lumberyard House Building Tracker facilitates managing house construction projects across multiple lumber yard locations. It includes CRUD for House Building Projects and House Rooms (tasks), with custom  and  for rooms and subtasks. A robust role-based access control system (, , ) ensures data isolation and hierarchical permissions.  has global control,  manages their store, and  has read-only project access. Real-time updates occur via polling, and users can attach files. A 2-level subtask system supports optional dates and color-coding. An Ideas Board is available for admin creation/editing. A Budget Tab allows customers to view overall project budgets, spending, and cost estimates, with the recent enhancement where project budget acts as a total from which room budgets are allocated. The system now supports viewing/reactivating deactivated users and super admin password resets, and auto-populates subtasks based on room types. Store-specific logos can be uploaded by super admins and displayed.
</product_requirements>

<key_technical_concepts>
-   **Full-stack Architecture**: React (frontend), FastAPI (backend), MongoDB (database).
-   **Role-Based Access Control (RBAC)**: SUPER_ADMIN, ADMIN, CUSTOMER roles.
-   **Multi-Tenancy**:  for data isolation.
-   **RESTful APIs**: Backend endpoints.
-   **Frontend State Management**: React Context ().
-   **Polling**: For real-time updates.
-   **Environment Variables**: , .
-   **Pydantic**: Data validation in FastAPI.
</key_technical_concepts>

<code_architecture>
The application utilizes a standard full-stack setup:



-   :
    -   **Importance**: Core FastAPI application handling API routes, MongoDB interaction, and business logic.
    -   **Changes**:
        -   Task, TaskCreate, TaskUpdate models updated with  and .
        -   ProjectCreate model updated with .
        -   New , , and user creation username conflict handling.
        -   New  and  endpoints.
        -    endpoint added for standard subtask templates.
        -   Budget rollup calculations added for projects and tasks, updating  and  endpoints with new calculated fields.
        -    and  models updated to include calculated budget fields.
        -   New  model and endpoints (, ) for logo management, with  returned on login.
        -   Refactored budget calculation logic to treat project budget as total, with rooms as allocations.
-   :
    -   **Importance**: Main React component managing routing and global layout.
    -   **Changes**:  component props updated to pass  and .
-   :
    -   **Importance**: Manages user authentication, session, and roles.
    -   **Changes**: Login function updated to pass  and now returns .
-   :
    -   **Importance**: Centralizes reusable UI components and main views.
    -   **Changes**:
        -    and  updated to include  field and handle its submission.
        -    and  updated to include  and  fields and handle their submission.
        -    component: Added  function and a Generate Standard Subtasks button.
        -    and  updated to display new calculated budget totals (, ,  for projects; ,  for tasks).
        -    component updated to include .
        -    component added to display store logos.
-   :
    -   **Importance**: User login interface.
    -   **Changes**: Updated to display  in the login header.
-   :
    -   **Importance**: Admin user management interface.
    -   **Changes**:
        -   Implemented a tab system for Active Users and Deactivated Users.
        -   Added  and  functions.
        -   User list actions updated to include reactivate button.
        -   Username conflict handling improved.
        -    component, along with generate/reset password functions, added for Super Admins. Reset password button added to user actions.
        -    component added for Super Admins to upload/manage store logos, with a Manage Store Logos button.
-   :
    -   **Importance**: Displays budget information for projects.
    -   **Changes**: Rewritten to include  based Pie and Bar charts for overall project budget and room-level breakdowns, using the project's estimated budget as the total and room budgets as allocations. Fixed API constant usage and added error handling.
-   :
    -   **Importance**: Global CSS styles for the application.
    -   **Changes**: Added styles for AdminPanel tabs, budget summaries, chart components, and logo elements.
</code_architecture>

<pending_tasks>
-   **Pinterest Integration**: Deferred by user, requires API credentials.
-   **Stripe Integration**: Mentioned in initial prompt, not yet requested.
-   **Slack Integration**: Mentioned in initial prompt, not yet requested.
-   **Project list scroll bar**: User requested adding a scroll bar to the project list for better visibility.
</pending_tasks>

<current_work>
The application has been continuously enhanced, transforming into a robust Lumberyard House Building Tracker. Most recently, the focus has been on comprehensive financial and user management features.

**Budgeting System Refinement**: The budget calculation logic was fundamentally changed. Previously, room budgets were added to the project budget. Now, the **house project budget is the total available funds**, and **room budgets are allocations that subtract from this total**. This change accurately reflects a top-down budgeting approach. The backend was updated with new calculation functions (, , ) and corresponding fields in the  model and API endpoints. The frontend  was completely rewritten to display this new logic using interactive **Pie and Bar charts**, showing allocated vs. spent, remaining budget, and a breakdown by rooms with percentages, all using the existing color scheme.  and  were also updated to display the new budget metrics.

**Store Logo Management**: A system for managing **store-specific logos** was implemented. Super Admins can now **upload and manage logos** for different stores via a new  in the . These logos are stored and dynamically displayed in the application's header/navigation ( component in ) and on the .

Immediately prior to this summary, the user identified that the **project list on the left sidebar only shows a limited number of projects with a X projects, +Y more projects indicator** and requested a scroll bar or dropdown for easier access to all projects. This is the next task to address.
</current_work>

<optional_next_step>
Implement a scroll bar or dropdown for the project list on the left side to show all projects directly.
</optional_next_step>
